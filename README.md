# a
a gem generator etc.

## install:
    gem install a
will create a `a` command
## example: use `a` create a gem 
    sudo a gem mytest
    cd mytest
    sudo rake run
for starter,  you need just type `a gem` 



## license:
MIT
## versions:
2018-10-17 init